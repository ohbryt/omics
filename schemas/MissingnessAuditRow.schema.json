{
  "$defs": {
    "QCDecision": {
      "enum": [
        "PASS",
        "FLAG",
        "EXCLUDE",
        "CANDIDATE"
      ],
      "title": "QCDecision",
      "type": "string"
    },
    "ValueClass": {
      "description": "Missing-value taxonomy (SPEC 3). Never collapse these.",
      "enum": [
        "biological_zero",
        "technical_zero",
        "below_lod",
        "true_missing",
        "unavailable_metadata",
        "imputed_placeholder",
        "not_applicable"
      ],
      "title": "ValueClass",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "entity_id": {
      "title": "Entity Id",
      "type": "string"
    },
    "value_class": {
      "$ref": "#/$defs/ValueClass"
    },
    "fraction": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Fraction"
    },
    "threshold": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Threshold"
    },
    "decision": {
      "$ref": "#/$defs/QCDecision"
    },
    "reason": {
      "default": "",
      "title": "Reason",
      "type": "string"
    }
  },
  "required": [
    "entity_id",
    "value_class",
    "decision"
  ],
  "title": "MissingnessAuditRow",
  "type": "object"
}